<Window x:Class="Launch.CommandWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Launch"
        xmlns:diag="clr-namespace:System.Diagnostics;assembly=WindowsBase"
        mc:Ignorable="d"
        Title="CommandWindow" Width="300" Topmost="True" WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False" WindowStartupLocation="CenterScreen" ScrollViewer.VerticalScrollBarVisibility="Disabled" SizeToContent="WidthAndHeight">
    <Border Padding="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="69*"/>
                <RowDefinition Height="3*"/>
                <RowDefinition Height="52*"/>
            </Grid.RowDefinitions>
            <Border HorizontalAlignment="Left" VerticalAlignment="Top" Width="300" CornerRadius="3" BorderBrush="#ECECEC" BorderThickness="1" Background="#EEEEEE">
                <TextBox x:Name="commandInput" TextWrapping="Wrap" AllowDrop="False" FontSize="24" FontFamily="游ゴシック" FontWeight="Bold" Padding="5,0" BorderThickness="0" Background="Transparent"/>
            </Border>
            <ListBox x:Name="suggestList" HorizontalAlignment="Left" MaxHeight="200" Margin="0,48,0,0" VerticalAlignment="Top" Width="300" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Hidden" BorderThickness="0" Background="Transparent" Grid.RowSpan="3" Visibility="Collapsed">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Width="300" Height="26" Orientation="Horizontal" Margin="0,1">
                            <Image Width="22" Height="22" Source="{Binding Icon}" Margin="2,2,4,2" />
                            <TextBlock Text="{Binding Name}" Margin="4,0,0,0" Width="270" Height="26" FontFamily="游ゴシック" FontSize="16" FontWeight="Medium"></TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Border>
</Window>
